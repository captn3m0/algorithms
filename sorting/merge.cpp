#include <math.h>
#include "../common.h"
int* merge(int* a,int start,int pivot,int end)
{
	int* x;
	return x;
}

void mergeSort(int* a,int start,int end)
{
	int r=floor((start+end)/2);
}

int main()
{
	int arr[5]={1,2,3,4,5};
	assertSorted(arr,5);
}